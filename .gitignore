# Build intermediates
Source/obj/
Source/bin/
**/bin/
**/obj/

# Debug symbols (regenerated by build; may be locked while game runs)
Assemblies/*.pdb
*.pdb

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
.vscode/

# OS junk
Thumbs.db
Desktop.ini
.DS_Store

# Local AI / agent session memory (not part of the mod)
.workbuddy/

# NuGet
*.nupkg
packages/
project.lock.json

# Optional local override for machine-specific RimWorld/Harmony paths
Directory.Build.props.user
*.local.props

# Agent/AI session memory (not part of the mod)
.codebuddy/

# Local debug / probe scratch scripts (not part of the mod; keep off VCS)
.tmp_check_ui.js
*.tmp
docs/ui-preview/_probe*.ps1
docs/ui-preview/_verify.cjs

# Local full-project snapshot backups (dated safety copies; never version-controlled)
_backup/
